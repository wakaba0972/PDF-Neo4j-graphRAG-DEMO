# GraphRAG 改善計畫

## 1. 背景與現況

目前問答流程如下：

```text
使用者問題
  → 向量與全文混合檢索
  → 依來源 chunk 與實體擴展圖譜
  → 將證據交給問答 LLM
```

目前的 GraphRAG 已具備基本的向量搜尋與圖譜證據擴展能力，但仍有以下限制：

- 混合檢索已完成，但候選證據尚未使用統一分數重新排序。
- 圖譜擴展主要依相同 chunk 或實體名稱取得資料，尚未針對問題進行有方向的關係遍歷。
- 擴展取得的證據沒有實際相關性分數，目前以 `0.0` 顯示，也沒有與原始向量結果一起重新排序。
- Top K 控制的是第一階段向量結果，GraphRAG 擴展後的實際證據可能超過 Top K。
- 缺少總 context token 預算，證據過多時可能增加雜訊或超過模型上下文。
- 每次問答只處理當前問題，尚未支援多輪追問及指代解析。
- 第一輪證據不足時不會自動改寫問題或進行第二次檢索。
- 已有自動生題、LLM 文件路由與答案判分；仍缺少人工固定基準集。

## 2. 改善目標

本計畫希望達成以下目標：

1. 提升正確證據被檢索到的機率。
2. 降低不相關或重複證據對回答的干擾。
3. 支援跨頁、實體關係及多輪追問問題。
4. 控制送入 LLM 的證據量、token 成本與回應時間。
5. 建立可重複執行的評測方式，以數據驗證每項改善。
6. 保留答案來源頁碼，並在證據不足時明確拒答。

## 3. 實施原則

- 先改善檢索精準度與可衡量性，再增加多跳遍歷等複雜功能。
- 所有檢索結果都必須能回溯至原文 chunk 與 PDF 頁碼。
- 新增行為時同步補充單元測試及整合測試。
- 每個階段完成後，以同一套評測集比較品質、速度與 token 使用量。
- 向量 RAG 保留為基準模式，方便與 GraphRAG 進行 A/B 比較。

## 4. 分階段改善項目

### P0：建立基準評測集與指標

> 實作狀態：自動評測第一版與可版本控制的基準報告工具已完成；30～50 題人工固定基準集尚未建立。

#### 工作項目

- 從實際 PDF 人工整理 30～50 題問答資料。
- 每題記錄：
  - 問題
  - 標準答案或答案要點
  - 正確來源頁碼
  - 正確來源 chunk（若可取得）
  - 問題類型
  - 文件是否具有足夠答案
- 題型至少包含：
  - 單一段落可回答的問題
  - 錯誤碼、型號、數值等精確查詢
  - 跨頁資訊整合
  - 實體關係、原因與影響分析
  - 文件中沒有答案的問題
  - 需要承接前文的多輪追問
- 建立可重複執行的評測程式或測試工具。

#### 評測指標

- `Recall@5`、`Recall@10`：正確頁面或 chunk 是否進入前 5／10 筆。
- 答案正確率或答案要點命中率。
- 引用頁碼正確率。
- 無答案問題的正確拒答率。
- 平均與 P95 回應時間。
- 每次問答送入模型的 context token 數。
- 向量 RAG 與 GraphRAG 的品質差異。

#### 驗收標準

- 評測資料格式固定且可版本控制。
- 同一組設定可重複執行並產生比較報告。
- 報告能區分向量 RAG 與 GraphRAG 結果。

### P1：新增全文與向量混合檢索

> 實作狀態：第一版已完成（CJK 全文索引、官方 HybridCypherRetriever、內建 naive ranker、run ID 隔離及 UI 官方混合分數顯示）。

#### 工作項目

- 為原文、實體名稱與關係文字建立 Neo4j 全文索引。
- 問答時平行取得：
  - 向量相似度候選
  - 全文／關鍵字候選
  - 實體名稱精確或模糊命中候選
- 使用官方 HybridCypherRetriever 與內建 ranker 合併結果。
- 保存官方混合分數與 Retriever 類型，方便除錯與顯示。
- 對錯誤碼、型號、英文縮寫等內容保留大小寫及符號處理策略。

#### 建議初始設定

```text
向量候選：20 筆
全文候選：20 筆
融合後候選：20～30 筆
最終送入回答階段：8～15 筆
```

#### 驗收標準

- 精確詞查詢的 `Recall@K` 不低於原本向量搜尋。
- 可以從問答結果查看證據來自向量、全文或兩者。
- 相同證據不會因不同檢索管道重複出現。

### P2：候選證據去重與重新排序

#### 工作項目

- 將向量、全文及圖譜擴展結果先組成候選集合，不直接交給 LLM。
- 依 `evidence_id`、來源 chunk 及文字相似度進行去重。
- 為所有候選計算統一分數，不再將擴展證據固定視為 `0.0`。
- 第一版先使用可解釋的加權排序，後續再評估 cross-encoder 或 LLM reranker。
- 在檢索來源介面顯示向量分數、全文分數、圖譜分數及最終分數。

#### 建議初始公式

```text
final_score =
  0.45 × vector_score
+ 0.25 × keyword_score
+ 0.20 × graph_score
+ 0.10 × source_proximity_score
```

實際權重應以評測集調整，不應視為固定規格。

#### 圖譜分數建議

- 問題直接命中的實體：高分。
- 命中實體的一跳關係：中高分。
- 關係的來源原文：中高分。
- 僅因相同 chunk 被擴展：中分。
- 兩跳關係：較低分，且須符合問題意圖。
- 無法回溯至原文來源：不送入回答模型。

#### 驗收標準

- 擴展證據具有可解釋的實際分數。
- 最終證據依統一分數排序。
- 重複證據比例下降，且評測答案品質不低於改善前。

### P3：加入 context token 預算

#### 工作項目

- 新增 `max_context_tokens` 設定。
- 依回答模型的 context window 預留：
  - system prompt
  - 問題與對話歷史
  - 回答輸出 token
- 依最終排序逐筆加入證據，超過預算即停止。
- 限制單一 chunk 最大長度，必要時截取與問題最相關的區段。
- 為原文、實體、關係設定各自的最低與最高比例，避免單一證據類型占滿上下文。
- 合併相鄰且重疊的原文 chunk。

#### 驗收標準

- 不會因 Top K 或圖譜擴展過大而超過模型 context window。
- 問答紀錄包含實際使用的證據數與估算 token 數。
- 在相同 token 預算下，GraphRAG 的正確率不低於原版本。

### P4：改為有方向的圖譜擴展

#### 工作項目

- 從高分實體或關係候選作為圖遍歷起點。
- 以 Cypher 取得：
  - 命中實體
  - 一跳關係
  - 鄰接實體
  - 支援該關係的原文 chunk
- 根據問題類型限制允許擴展的關係。
- 預設只擴展一跳；只有比較、原因、影響或路徑型問題才考慮兩跳。
- 對高度數節點降權，避免通用實體帶回大量無關資料。
- 為每個起始實體限制最大關係數及總擴展數。

#### 建議流程

```text
混合檢索命中實體
  → 一跳關係
  → 鄰接實體
  → 關係的來源原文
  → 統一 rerank
  → 依 token 預算組裝 context
```

#### 驗收標準

- 圖譜擴展結果能說明遍歷起點、關係與距離。
- 所有關係證據皆能回溯到來源頁碼或 chunk。
- 關係型題目的正確率高於向量 RAG 基準。
- 一般事實型問題不會因圖譜擴展而明顯降低品質。

### P5：支援多輪問答與問題改寫

#### 工作項目

- 保存最近 3～5 輪使用者問題與回答摘要。
- 檢索前將含有代名詞或省略資訊的追問改寫為獨立問題。
- 同時保存原始問題與改寫後問題，方便追蹤與除錯。
- 對話歷史納入 token 預算，不直接無限制累積。
- 新對話或切換文件時清除不相關歷史。

#### 範例

```text
上一題：E01 應該如何處理？
追問：那如果還是沒有恢復呢？

改寫後：依照 E01 的排除步驟處理後，設備仍未恢復時，下一步應如何處理？
```

#### 驗收標準

- 多輪評測題可正確解析代名詞與省略資訊。
- 改寫不應加入對話或文件中不存在的前提。
- 使用者可清除問答歷史。

### P6：證據充分性判斷與二次檢索

#### 工作項目

- 第一輪檢索後判斷證據是否足以回答問題。
- 證據不足時，執行以下一種或多種策略：
  - 抽取問題中的關鍵實體
  - 產生同義詞或精確搜尋詞
  - 將複合問題拆成子問題
  - 改用全文、向量或圖譜檢索
  - 擴展至第二跳關係
- 合併兩輪候選後重新去重、排序及套用 token 預算。
- 設定最多檢索輪數，避免成本與延遲失控。
- 仍無足夠證據時，明確回答文件未提供足夠資訊。

#### 驗收標準

- 每次二次檢索都有可記錄的觸發原因。
- 無答案問題的臆測比例下降。
- 回應時間與模型呼叫次數維持在設定上限內。

### P7：依問題類型選擇檢索策略

#### 工作項目

- 新增輕量問題分類器或規則判斷。
- 依問題類型選擇檢索方式：

| 問題類型 | 建議策略 |
| --- | --- |
| 錯誤碼、型號、數值 | 全文＋向量檢索 |
| 某實體的定義或說明 | Local GraphRAG |
| 原因、影響、相互關係 | 實體起點＋關係遍歷 |
| 整份文件的主題或趨勢 | Community summary／Global Search |
| 多輪追問 | 問題改寫後再檢索 |

- 將自動選擇結果顯示在問答紀錄中。
- 保留手動指定模式，方便測試及比較。

#### 驗收標準

- 分類器在評測題型上的準確率達到預定門檻。
- 自動模式的整體答案品質高於固定單一策略。
- 使用者仍可選擇向量 RAG 或 GraphRAG 作為基準模式。

## 5. 建議的第一版實作範圍

第一輪迭代建議只完成以下項目：

1. 建立 30～50 題評測集及基準報告。
2. 新增 Neo4j 全文索引與混合檢索。
3. 將所有候選統一去重、評分及重新排序。
4. 新增 `max_context_tokens` 與證據組裝策略。
5. 保存最近數輪對話並改寫追問。

這一版完成後，再依評測結果判斷是否需要實作兩跳遍歷、二次檢索及 Community／Global Search，避免在缺少量化結果前增加過多系統複雜度。

## 6. 建議模組調整

可依目前專案結構逐步拆分責任：

| 模組 | 建議調整 |
| --- | --- |
| `qa_service.py` | 問題改寫、token 預算、context 組裝、充分性判斷與回答生成 |
| `neo4j_service.py` | 全文索引、混合檢索、圖遍歷及候選來源分數 |
| `ui.py` | 自動／手動模式、進階參數、檢索過程與分數顯示、清除對話 |
| 新增 `retrieval_service.py` | 候選融合、去重、rerank 與檢索策略選擇 |
| 新增 `evaluation.py` | 評測資料載入、批次執行、指標計算與報告輸出 |
| `tests/` | 混合檢索、排序、token 預算、多輪改寫及無答案行為測試 |

是否拆出新模組可依第一版實作規模決定；重點是避免把所有檢索策略繼續集中在單一函式中。

## 7. 測試計畫

### 單元測試

- 官方 HybridCypherRetriever 的融合排序與參數傳遞。
- 不同來源候選的去重。
- 圖譜距離與來源接近度分數。
- token 預算裁切與證據類型配額。
- 多輪問題改寫輸入與失敗處理。
- 證據不足及第二輪檢索觸發條件。

### Neo4j 整合測試

- 全文索引建立與查詢。
- 混合檢索的 run ID 隔離。
- 一跳關係與來源 chunk 回溯。
- 高度數節點及最大擴展數限制。
- 空資料庫、索引不存在及查詢錯誤處理。

### UI 測試

- 檢索模式與進階參數傳遞正確。
- 檢索來源表格能顯示各類分數。
- 多輪歷史保存與清除。
- 錯誤訊息不會清除已存在的有效狀態。

### 回歸評測

- 每個階段完成後執行固定評測集。
- 與改善前的向量 RAG、GraphRAG 基準比較。
- 若核心指標下降，應先找出退步題型，不直接宣告改善完成。

## 8. 里程碑

| 里程碑 | 內容 | 完成條件 |
| --- | --- | --- |
| M1 | 評測基線 | 評測集、批次執行與基準報告完成 |
| M2 | 混合檢索 | 全文＋向量候選可融合、去重並顯示來源 |
| M3 | 排序與預算 | 擴展證據可評分，context 不超過 token 上限 |
| M4 | 多輪問答 | 最近對話可用於獨立問題改寫與檢索 |
| M5 | 圖譜深化 | 有方向的一跳遍歷通過關係型題目評測 |
| M6 | 自適應檢索 | 證據不足可二次搜尋，並能依題型選擇策略 |
| M7 | 全域問答 | 視需求加入社群摘要與 Global Search |

## 9. 風險與控制方式

| 風險 | 控制方式 |
| --- | --- |
| 圖譜擴展帶入大量雜訊 | 限制跳數、每節點關係數，並統一 rerank |
| 全文與向量結果重複 | 以 evidence ID、chunk 及文字相似度去重 |
| context 超限 | 強制 token 預算並預留回答空間 |
| LLM 問題改寫產生錯誤前提 | 保存原始問題、限制改寫用途並加入測試 |
| 二次檢索造成延遲與成本增加 | 設定觸發門檻、最大輪數與逾時 |
| 建圖抽取品質限制檢索上限 | 追蹤實體／關係抽取準確率並保留原文兜底 |
| 指標改善但實際體感下降 | 同時檢查離線評測、人工審查與回應時間 |

## 10. 成功條件

整體改善可視為成功，需同時符合：

- 正確來源的 `Recall@K` 相較基準提升。
- 關係型與跨頁問題的答案正確率提升。
- 引用頁碼維持正確且可追溯。
- 無答案問題不會因圖譜擴展而增加臆測。
- 問答 context 不超過設定的 token 預算。
- 多輪追問不必由使用者反覆補充完整主詞。
- 延遲與模型成本在可接受範圍內。
- 向量 RAG、GraphRAG 及自動策略能以相同評測集持續比較。
